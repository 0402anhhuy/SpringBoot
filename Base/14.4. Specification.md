# 🌱 Hibernate Specification

## 🍯 Khái niệm

- Là một **Design Pattern** thuộc Spring Data JPA, dựa trên **JPA Criteria API**

- Nó cho phép bạn **xây dựng truy vấn động (dynamic query)** một cách linh hoạt

- Mỗi `Specification<T>` đại diện cho **một điều kiện** trong mệnh đề `WHERE`

### 📍 Ví dụ

`Specification.hasName("Iphone")` → tương đương `WHERE name LIKE '%Iphone%'`

`Specification.priceGreaterThan(1000)` → tương đương `WHERE price > 1000`

## 🍱 Cách sử dụng

- Entity (VD: `Product`)

- Repository kế thừa `JpaSpecificationExecutor<T>`

- Viết các `Specification` (lambda hoặc class riêng)

- Kết hợp các `Specification` khi gọi repository

## 🍲 Ví dụ cụ thể

### 🍣 Entity

```java
@Entity
@Data
public class Product {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String name;
    private Double price;
    private String category;

}
```

### 🧂 Repository

```java
public interface ProductRepository extends JpaRepository<Product, Long>, JpaSpecificationExecutor<Product> {
}
```

`JpaSpecificationExecutor` giúp repo có method `findAll(Specification<T>)`

### 🍢 Tạo Specification

```java
public class ProductSpecifications {

    public static Specification<Product> hasName(String name) {
        return (root, query, cb) -> name == null ? null : cb.like(root.get("name"), "%" + name + "%");
    }

    public static Specification<Product> hasCategory(String category) {
        return (root, query, cb) -> category == null ? null : cb.equal(root.get("category"), category);
    }

    public static Specification<Product> priceGreaterThan(Double price) {
        return (root, query, cb) -> price == null ? null : cb.greaterThan(root.get("price"), price);
    }
}
```

### 🍤 Service

```java
@Service
public class ProductService {
    @Autowired
    private ProductRepository productRepository;

    public List<Product> searchProducts(String name, String category, Double minPrice) {
        return productRepository.findAll(
                Specification.where(ProductSpecifications.hasName(name))
                        .and(ProductSpecifications.hasCategory(category))
                        .and(ProductSpecifications.priceGreaterThan(minPrice))
        );
    }
}
```

### 🍚 Controller

```java
@RestController
@RequestMapping("/products")
public class ProductController {
    @Autowired
    private ProductService productService;

    @GetMapping("/search")
    public List<Product> searchProducts(
            @RequestParam(required = false) String name,
            @RequestParam(required = false) String category,
            @RequestParam(required = false) Double minPrice
    ) {
        return productService.searchProducts(name, category, minPrice);
    }
}
```

### 🧆 Demo Request

- `GET /products/search?name=Iphone` → Tìm sản phẩm có tên chứa "Iphone"
- `GET /products/search?category=Electronics&minPrice=500` → Tìm sản phẩm thuộc Electronics, giá > 500
- `GET /products/search` → Không truyền gì, lấy tất cả